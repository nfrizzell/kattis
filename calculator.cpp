#include <iostream>
#include <algorithm>
#include <string>
#include <vector>

int main()
{
	std::string line;
	std::cin >> line;
	while (line != "EOF")
	{
		auto open_parenthesis = std::vector
		long double output = 0.0;
		line.erase(std::remove_if(line.begin(), line.end(), ::isspace), line.end());
	
		std::cin >> line;
	}
	
}
